name: "Copilot Setup Steps"

# Allow testing of the setup steps from your repository's "Actions" tab.
on: workflow_dispatch

jobs:
  # The job MUST be called `copilot-setup-steps` or it will not be picked up by Copilot.
  copilot-setup-steps:
    runs-on: ubuntu-24.04 # ubuntu-latest
    # env:
      # Hardcoded non-sensitive Azure identifiers.
      # FOUNDRY_ENDPOINT: https://squintelier-5556-resource.services.ai.azure.com/

    # Set the permissions to the lowest permissions possible needed for your steps.
    # Copilot will be given its own token for its operations.
    permissions:
      # If you want to clone the repository as part of your setup steps, for example to install dependencies, you'll need the `contents: read` permission. If you don't clone the repository in your setup steps, Copilot will do this for you automatically after the steps complete.
      contents: write
      id-token: write

    # You can define any steps you want, and they will run before the agent starts.
    # If you do not check out your code, Copilot will do this for you.
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Cache Hugo resources
        uses: actions/cache@v6
        env:
            cache-name: cache-hugo-resources
        with:
            path: resources
            key: ${{ env.cache-name }}

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
            go-version: "^1.23.3"

      - name: Check Go version
        run: go version

      - name: Set up Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: "20"
          cache: "npm"

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip openai azure-identity

      # - name: Validate Azure auth inputs
      #   shell: bash
      #   run: |
      #     set -euo pipefail
      #     client_id="2d9952c6-0a40-4dc1-bc60-e1bd069e11fd"
      #     tenant_id="2dfe981e-73de-4774-96bf-57b42ca243d8"
      #     subscription_id="27ab9d9b-03ae-4560-9e84-16cfc5dbfb08"

      #     if [ -z "$client_id" ] || [ -z "$tenant_id" ] || [ -z "$subscription_id" ]; then
      #       echo "::error::Azure OIDC preflight failed. One or more hardcoded Azure identifiers are empty."
      #       exit 1
      #     fi

      #     echo "Azure auth input preflight passed."

      # - name: Azure login (OIDC)
      #   uses: azure/login@v3
      #   with:
      #     client-id: 2d9952c6-0a40-4dc1-bc60-e1bd069e11fd
      #     tenant-id: 2dfe981e-73de-4774-96bf-57b42ca243d8
      #     subscription-id: 27ab9d9b-03ae-4560-9e84-16cfc5dbfb08

      # - name: Validate Azure session
      #   shell: bash
      #   run: |
      #     set -euo pipefail
      #     az account show 1>/dev/null
      #     echo "Azure OIDC login succeeded."

      # - name: Validate Foundry token access
      #   shell: bash
      #   run: |
      #     set -euo pipefail
      #     token=$(az account get-access-token --resource https://cognitiveservices.azure.com/ --query accessToken -o tsv)
      #     if [ -z "$token" ]; then
      #       echo "::error::Unable to acquire Azure AI token for Foundry resource scope."
      #       exit 1
      #     fi
      #     echo "Foundry token preflight succeeded for ${FOUNDRY_ENDPOINT}."

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
            hugo-version: "0.147.3"
            extended: true

      - name: Install JavaScript dependencies
        run: npm ci
