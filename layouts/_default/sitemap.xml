{{- $pages := slice }}
{{- $pages = $pages | append .Site.Pages }}

{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range $pages }}
    {{- $robots := lower (default "index, follow" .Params.robots) }}
    {{- $filePath := "" }}
    {{- $isToolsRoot := false }}
    {{- with .File }}
      {{- $filePath = replace .Path "\\" "/" }}
      {{- $isToolsRoot = eq $filePath "tools/_index.md" }}
    {{- end }}
    {{- if and (not $isToolsRoot) (not (in $robots "noindex")) }}
      <url>
        <loc>{{ .Permalink }}</loc>
        {{- with .Lastmod }}
          <lastmod>{{ .UTC.Format "2006-01-02T15:04:05Z" }}</lastmod>
        {{- end }}
        <changefreq>monthly</changefreq>
        <priority>{{ if eq .Type "tools" }}0.6{{ else }}0.5{{ end }}</priority>
        <!--
        <info>
          <kind>{{ .Kind }}</kind>
          <filePath>{{ $filePath }}</filePath>
          <type>{{ .Type }}</type>
        </info>
        -->
      </url>
    {{- end }}
  {{- end }}
</urlset>
